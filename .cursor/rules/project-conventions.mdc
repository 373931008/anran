---
description: anran 主题项目通用开发规范 - 适用于所有文件
alwaysApply: true
---

# anran 主题开发通用规范

本规范适用于整个 anran Shopify 主题项目的所有代码文件。

## 技术栈

- **框架**: Shopify Liquid
- **样式**: 原生 CSS（可配合项目现有样式）
- **构建**: Vite
- **轮播**: Swiper.js (Web Component)
- **MCP 工具**:
  - `user-Figma`: Figma 设计稿读取
  - `user-shopify-dev-mcp`: Shopify 代码验证
  - `user-swiper`: Swiper 官方示例和文档

## 代码风格

### 1. 使用中文注释

```liquid
{%- comment -%}
  功能：产品轮播展示
  描述：使用 Swiper 实现产品图片轮播
{%- endcomment -%}
```

### 2. 文件命名规范

- **Sections**: `q-feature-name.liquid`（q- 前缀）
- **Snippets**: `kebab-case.liquid`
- **Assets**: `kebab-case.js` / `kebab-case.css`

### 3. 代码组织

```liquid
{%- comment -%}注释{%- endcomment -%}

<style>
  /* 组件专属样式 */
</style>

<div class="section-name">
  <!-- 主要内容 -->
</div>

<script>
  // JavaScript 逻辑
</script>

{% schema %}
{
  "name": "Section Name",
  "settings": []
}
{% endschema %}
```

## 自动化测试规范

> **强制要求**：每次开发完成后，必须使用 `user-chrome-devtools` MCP 对 `http://127.0.0.1:9292/` 进行自动测试，确认无报错、无上传失败、页面渲染正确后方可结束任务。

### 标准测试流程

```javascript
// 1. 刷新页面（强制忽略缓存）
navigate_page({ type: "url", url: "http://127.0.0.1:9292/", ignoreCache: true })

// 2. 截图检查页面渲染
take_screenshot({ format: "png" })

// 3. 抓取 error/warn 级别 console 消息
list_console_messages({ types: ["error", "warn"] })
```

### 错误判断标准

| 错误来源 | 处理方式 |
|---|---|
| Shopify Upload Errors（红色横幅）| **必须修复**，检查 schema / Liquid 语法 |
| `sections/*.liquid` 报错 | **必须修复** |
| `templates/*.json` 报错 | **必须修复** |
| `https://0d0ac8.myshopify.com/api/collect` CORS | **忽略**，Shopify Analytics 本地跨域，预期行为 |
| `shop.app` CSP frame-ancestors | **忽略**，Shopify 自有 iframe，预期行为 |
| `shopify-perf-kit` / `Uncaught (in promise)` | **忽略**，Shopify 性能追踪脚本在本地失败，预期行为 |
| `Ecom popup data element not found` | **忽略**，第三方 App 组件 |
| 我们自己代码产生的 JS 错误 | **必须修复** |

### Swiper 特定检查

```javascript
// 确认无 loop mode 警告（slides 不足时不应开启 loop）
// 确认 realIndex、激活圆点、fill 动画三值同步
evaluate_script({
  function: "() => { const swiper = document.querySelector('swiper-container')?.swiper; return { realIndex: swiper?.realIndex } }"
})
```

---

## 性能规范

> **强制要求**：所有新增代码必须遵守以下性能规范，避免 CLS（累积布局偏移）、不必要的 Layout/Paint，确保首屏体验和长列表滚动流畅。

### 1. 图片加载策略

```liquid
{%- comment -%} ✅ 首屏图片（Hero、第一屏）：eager + 高优先级 {%- endcomment -%}
{{- image | image_url: width: 1920 | image_tag:
  loading: 'eager',
  fetchpriority: 'high',
  widths: '750,1100,1500,1920',
  sizes: '100vw',
  alt: ''
-}}

{%- comment -%} ✅ 非首屏图片（产品卡片、推荐区等）：lazy {%- endcomment -%}
{{- image | image_url: width: 800 | image_tag:
  loading: 'lazy',
  widths: '375,750,1100',
  sizes: '(min-width: 750px) 33vw, 50vw',
  alt: ''
-}}

{%- comment -%} ❌ 禁止：手写 <img> 或省略 loading/widths/sizes {%- endcomment -%}
```

**判断原则**：
- 在 `templates/index.json` 中 `order` 里排在**第 1 位**的 section → `loading: 'eager'`
- 其余所有 section 内图片 → `loading: 'lazy'`
- 轮播中第 1 张 slide 的背景图 → `loading: 'eager'`，其余 slides → `loading: 'lazy'`

### 2. JavaScript 懒加载（Islands 架构）

```liquid
{%- comment -%} ✅ 所有交互组件必须使用 client:visible（进入视口时才初始化） {%- endcomment -%}
<my-component-element client:visible>
  ...
</my-component-element>

{%- comment -%} ❌ 禁止：省略 client:visible（组件随页面立即执行）{%- endcomment -%}
<my-component-element>
  ...
</my-component-element>
```

**例外**：Hero Banner 等首屏交互组件**可以**使用 `client:visible`（首屏立即可见，observer 会立刻触发），无需特殊处理。

### 3. CSS 动画性能

```css
/* ✅ 优先使用 transform / opacity（GPU Compositor 线程，不触发 Layout/Paint） */
.fill { transform: scaleX(var(--progress)); will-change: transform; }
.fade { opacity: 0; transition: opacity 0.3s; }

/* ❌ 避免对 width/height/top/left 做动画（触发 Layout → Paint → Composite） */
.bad { width: 0%; transition: width 5s; }
```

**规则**：
- 进度条、展开/收起等动画 → 用 `transform: scaleX() / scaleY()` 代替 `width / height`
- 淡入淡出 → 用 `opacity`，配合 `visibility: hidden` 控制交互
- `will-change` 仅在动画元素上使用，动画结束后考虑用 JS 移除，避免长期占用 GPU 合成层

### 4. CLS（累积布局偏移）防护

```css
/* ✅ 详见 .cursor/rules/section-development.mdc「十三」：宿主 display:flex + slide flex-shrink:0；轨道间距只用 spaceBetween，勿再用 CSS gap 重复 */
.section-{{ section.id }} swiper-container {
  width: 100%;
  display: flex;
  min-width: 0;
}
.section-{{ section.id }} swiper-slide {
  flex-shrink: 0;
}

/* ✅ 图片容器预留宽高比，避免图片加载后撑开页面 */
.card__image-wrapper {
  aspect-ratio: 4 / 3; /* 或使用 padding-top 百分比方式 */
  overflow: hidden;
}
```

### 5. 视频懒加载

```liquid
{%- comment -%} ✅ 非首屏视频：不自动播放，进入视口后才加载 {%- endcomment -%}
<video-element client:visible>
  {{- video | video_tag:
    autoplay: false,
    loop: true,
    muted: true,
    playsinline: true,
    loading: 'lazy'
  -}}
</video-element>

{%- comment -%} 或对背景视频使用 preload="none" {%- endcomment -%}
<video preload="none" data-src="..." muted loop playsinline></video>
```

### 6. 开发完成后性能自检清单

- [ ] 所有非首屏图片设置了 `loading: 'lazy'`
- [ ] 首屏图片设置了 `loading: 'eager'` 且有 `fetchpriority: 'high'`
- [ ] 所有交互 Islands 使用了 `client:visible`
- [ ] CSS 动画使用 `transform` / `opacity` 而非 `width` / `height`
- [ ] `swiper-container` 有 `width: 100%; display: flex; min-width: 0`；`swiper-slide` 有 `flex-shrink: 0`；全宽卡时按需 `width: 100%`；**不要**在 `swiper-container` 上再写与 `spaceBetween` 等价的 `gap`
- [ ] 图片容器有明确的宽高比（`aspect-ratio` 或 `padding-top`）
- [ ] `will-change` 只在真正需要的动画元素上使用

---

## MCP 工具使用

### Figma 设计稿

从 Figma 链接提取设计：

```javascript
// 1. 获取元数据
get_metadata({ fileKey, nodeId })

// 2. 获取设计截图
get_screenshot({ fileKey, nodeId })

// 3. 生成代码
get_design_context({ fileKey, nodeId })
```

### Shopify 验证

验证 Liquid 代码：

```javascript
// 1. 初始化
learn_shopify_api()

// 2. 验证文件
validate_theme({
  conversationId,
  absoluteThemePath,
  filesCreatedOrUpdated: [{ path }]
})
```

### Swiper 示例

获取轮播示例代码：

```javascript
get_demo({
  slug: "navigation",
  framework: "element"
})
```

## 提交规范

### Git Commit 消息

```bash
feat: 添加产品轮播 Section
fix: 修复移动端布局问题
style: 优化按钮样式
refactor: 重构图片加载逻辑
docs: 更新开发文档
```

### 类型说明

- `feat`: 新功能
- `fix`: Bug 修复
- `style`: 样式调整
- `refactor`: 代码重构
- `docs`: 文档更新
- `perf`: 性能优化
- `test`: 测试相关

## 开发流程

1. **需求分析** - 理解功能需求和设计稿
2. **技术选型** - 选择合适的技术方案（Swiper 等）
3. **开发实现** - 编写 Liquid + CSS
4. **代码验证** - 使用 Shopify Dev MCP 验证
5. **测试检查** - 移动端 + 桌面端测试
6. **提交代码** - 遵循 Commit 规范

## 注意事项

- ✅ 使用 `container` 控制内容宽度
- ✅ Schema 配置提供有效默认值
- ✅ 轮播优先使用 `swiper-container`
- ✅ 响应式设计（移动端优先）
- ✅ 图片优化（lazy loading、responsive sizes）
- ❌ 避免内联样式
- ❌ 避免硬编码尺寸（使用 CSS 类或变量）
- ❌ 避免复杂的自定义 JavaScript（优先使用 Swiper）
