---
description: Siipet 主题新增板块（Section）完整开发规范，包含文件结构、CSS 隔离、Islands 架构、Schema 配置、通用 snippet 使用方法
alwaysApply: false
globs: sections/q-*.liquid,sections/main-product*.liquid,snippets/*.liquid,frontend/islands/*.js
---

# Siipet 新增板块开发规范

## 文件清单

新增一个交互板块需要创建/修改以下文件：

| 文件 | 说明 |
|------|------|
| `sections/q-{name}.liquid` | 板块主文件（HTML + CSS + Schema） |
| `frontend/islands/{name}-element.js` | JavaScript 交互逻辑（Islands 架构） |
| `templates/*.json` | 将板块加入页面模板的 `sections` 和 `order` |

> **文件命名规则**：islands 文件名必须与自定义元素标签名保持一致。自定义元素统一使用 `{name}-element` 格式（如 `<smart-detection-element>`），对应文件名为 `smart-detection-element.js`。

纯展示类板块（无 JS 交互）无需创建 islands 文件。

---

## 一、Section 文件结构

> ⚠️ **强制规范**：所有板块头部（标题 + 描述）**必须**使用 `q-section-header` snippet，**禁止**自行编写标题/描述 HTML 和 CSS。

```liquid
{%- comment -%}
  Section: q-{name}
  功能说明：...
  技术栈：
  - Islands: frontend/islands/{name}-element.js
  - Snippet: snippets/q-section-header.liquid
  版本：1.0.0
{%- endcomment -%}

<style>
  .section-{{ section.id }} {
    --prefix-bg: {{ section.settings.bg_color }};
    --prefix-padding-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --prefix-padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;
    --prefix-padding-top-desktop: {{ section.settings.padding_top_desktop }}px;
    --prefix-padding-bottom-desktop: {{ section.settings.padding_bottom_desktop }}px;
  }

  .section-{{ section.id }}.q-{name} {
    background: var(--prefix-bg);
    padding-top: var(--prefix-padding-top-mobile, 40px);
    padding-bottom: var(--prefix-padding-bottom-mobile, 40px);
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}.q-{name} {
      padding-top: var(--prefix-padding-top-desktop, 60px);
      padding-bottom: var(--prefix-padding-bottom-desktop, 60px);
    }
  }
</style>

<div class="q-{name} section-{{ section.id }}">
  <div class="page-width">
    {%- render 'q-section-header', section: section -%}
    <{name}-element client:visible>
      <!-- 内容 -->
    </{name}-element>
  </div>
</div>

{% schema %}
{
  "name": "Q-{Name}",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    { "type": "header", "content": "板块标题" },
    { "type": "inline_richtext", "id": "header_title", "label": "标题", "default": "Section Title" },
    { "type": "richtext", "id": "header_description", "label": "描述" },
    { "type": "checkbox", "id": "header_center", "label": "居中对齐", "default": false },
    { "type": "color_background", "id": "header_em_color", "label": "em 高亮色", "default": "#FF6B35" },
    { "type": "range", "id": "header_title_size_desktop", "label": "标题字号（桌面端）", "min": 20, "max": 80, "step": 2, "unit": "px", "default": 42 },
    { "type": "range", "id": "header_title_size_mobile", "label": "标题字号（移动端）", "min": 16, "max": 50, "step": 2, "unit": "px", "default": 26 },
    { "type": "range", "id": "header_desc_size_desktop", "label": "描述字号（桌面端）", "min": 10, "max": 30, "step": 1, "unit": "px", "default": 16 },
    { "type": "range", "id": "header_desc_size_mobile", "label": "描述字号（移动端）", "min": 10, "max": 24, "step": 1, "unit": "px", "default": 12 },
    { "type": "range", "id": "header_margin_bottom_desktop", "label": "标题下边距（桌面端）", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 40 },
    { "type": "range", "id": "header_margin_bottom_mobile", "label": "标题下边距（移动端）", "min": 0, "max": 80, "step": 4, "unit": "px", "default": 24 },
    { "type": "header", "content": "板块背景" },
    { "type": "color_background", "id": "bg_color", "label": "背景色", "default": "#FFFFFF" },
    { "type": "header", "content": "上下边距" },
    { "type": "range", "id": "padding_top_mobile", "label": "上边距（移动端）", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 40 },
    { "type": "range", "id": "padding_bottom_mobile", "label": "下边距（移动端）", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 40 },
    { "type": "range", "id": "padding_top_desktop", "label": "上边距（桌面端）", "min": 0, "max": 200, "step": 4, "unit": "px", "default": 60 },
    { "type": "range", "id": "padding_bottom_desktop", "label": "下边距（桌面端）", "min": 0, "max": 200, "step": 4, "unit": "px", "default": 60 }
  ],
  "blocks": [ ... ],
  "presets": [{ "name": "Q-{Name}" }]
}
{% endschema %}
```

---

## 二、CSS 隔离规则（必须遵守）

**所有样式必须加 `.section-{{ section.id }}` 前缀：**

```css
/* ❌ 错误 - 会污染其他同类板块 */
.q-faq__tab-btn { ... }

/* ✅ 正确 - 板块间完全隔离 */
.section-{{ section.id }} .q-faq__tab-btn { ... }

/* ✅ 根元素（自身同时拥有两个类，无空格） */
.section-{{ section.id }}.q-faq { ... }
```

**可配置的值通过 CSS 变量传递：**

```liquid
<style>
  .section-{{ section.id }} {
    --prefix-bg: {{ section.settings.bg_color }};
    --prefix-padding-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --prefix-padding-top-desktop: {{ section.settings.padding_top_desktop }}px;
  }
</style>
```

---

## 三、Islands 架构（JS 交互）

所有 JS 交互必须使用自定义元素，文件放 `frontend/islands/`，**不要在 Liquid 中写 `<script>` 或用 `asset_url` 引入**，revive.js 会自动加载。

支持的 client 指令：

| 指令 | 触发时机 | 适用场景 |
|------|---------|---------|
| `client:visible` | 元素进入视口时 | **默认选择**，所有板块使用此指令 |

### Islands 文件模板

```javascript
// frontend/islands/{name}-element.js
export default class MyElement extends HTMLElement {
  constructor() {
    super()
    // 绑定方法，保证 removeEventListener 能正确移除
    this.handleClick = this.handleClick.bind(this)
  }

  connectedCallback() {
    this.init()
  }

  disconnectedCallback() {
    this.cleanup()
  }

  init() {
    // 事件委托（优先）而非对每个子元素单独绑定
    this.addEventListener('click', this.handleClick)
  }

  cleanup() {
    this.removeEventListener('click', this.handleClick)
  }

  handleClick(event) {
    const target = event.target.closest('.selector')
    if (!target) return
    // 业务逻辑
  }
}

// 防重复注册
if (!window.customElements.get('{name}-element')) {
  window.customElements.define('{name}-element', MyElement)
}
```

### Liquid 中使用自定义元素

```liquid
<{name}-element client:visible>
  <!-- 内容由 Liquid 渲染，JS 负责交互 -->
</{name}-element>
```

### slide down/up 动画标准实现

展开（slide down）：
```javascript
openItem(item, animate) {
  const wrap = item.querySelector('.answer-wrap')
  item.setAttribute('data-open', '')
  if (animate) {
    wrap.style.transition = 'max-height 0.3s ease'
    wrap.style.maxHeight = wrap.scrollHeight + 'px'
    wrap.addEventListener('transitionend', () => {
      wrap.style.maxHeight = '2000px' // 防止内容变化截断
      wrap.style.transition = ''
    }, { once: true })
  } else {
    wrap.style.transition = ''
    wrap.style.maxHeight = '2000px'
  }
}
```

收起（slide up）— **必须先强制重绘再动画，否则从 2000px 收起时动画丢失**：
```javascript
closeItem(item, animate) {
  const wrap = item.querySelector('.answer-wrap')
  item.removeAttribute('data-open')
  if (animate) {
    wrap.style.transition = 'none'
    wrap.style.maxHeight = wrap.scrollHeight + 'px'
    wrap.offsetHeight // 强制重绘
    wrap.style.transition = 'max-height 0.3s ease'
    wrap.style.maxHeight = '0'
  } else {
    wrap.style.transition = 'none'
    wrap.style.maxHeight = '0'
  }
}
```

---

## 四、通用头部 Snippet

> ⚠️ **强制规范**：所有板块头部**必须**使用此 snippet，**禁止**自行编写标题/描述 HTML 和样式。

调用方式（在 `page-width` 容器内、内容区之前）：

```liquid
{%- render 'q-section-header', section: section -%}
```

**必须在 section schema 的 settings 中加入以下字段（复制粘贴，不能省略）：**

```json
{ "type": "header", "content": "板块标题" },
{ "type": "inline_richtext", "id": "header_title", "label": "标题", "default": "Title" },
{ "type": "richtext", "id": "header_description", "label": "描述" },
{ "type": "checkbox", "id": "header_center", "label": "居中对齐", "default": false },
{ "type": "color_background", "id": "header_em_color", "label": "em 高亮色", "default": "#FF6B35" },
{ "type": "range", "id": "header_title_size_desktop", "label": "标题字号（桌面端）", "min": 20, "max": 80, "step": 2, "unit": "px", "default": 42 },
{ "type": "range", "id": "header_title_size_mobile", "label": "标题字号（移动端）", "min": 16, "max": 50, "step": 2, "unit": "px", "default": 26 },
{ "type": "range", "id": "header_desc_size_desktop", "label": "描述字号（桌面端）", "min": 10, "max": 30, "step": 1, "unit": "px", "default": 16 },
{ "type": "range", "id": "header_desc_size_mobile", "label": "描述字号（移动端）", "min": 10, "max": 24, "step": 1, "unit": "px", "default": 12 },
{ "type": "range", "id": "header_margin_bottom_desktop", "label": "标题下边距（桌面端）", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 40 },
{ "type": "range", "id": "header_margin_bottom_mobile", "label": "标题下边距（移动端）", "min": 0, "max": 80, "step": 4, "unit": "px", "default": 24 }
```

> `em` 标签文字会自动应用 `header_em_color` 渐变/纯色，`color_background` 类型兼容单色和渐变。

---

## 五、Schema 标准配置项

### 图片与视频输出规范（必须遵守）

- **图片**：一律使用 `image_url` + `image_tag`，避免手写 `<img>`。

```liquid
{%- if section.settings.image != blank -%}
  {{-
    section.settings.image
    | image_url: width: 800
    | image_tag:
      loading: 'lazy',
      widths: '400,600,800,1000',
      alt: section.settings.image_alt
  -}}
{%- endif -%}
```

- **视频**：一律使用 `video_tag` 并通过 `image_size` 控制尺寸，不手动拼接 `<video>` 标签。

```liquid
{%- if section.settings.video != blank -%}
  {{-
    section.settings.video
    | video_tag:
      class: 'q-section__video',
      image_size: '800x',
      autoplay: false,
      loop: true,
      muted: true,
      playsinline: true,
      controls: true
  -}}
{%- endif -%}
```

- 只有在 `video_tag` 无法满足需求（例如需要逐个遍历 `sources` 做特殊逻辑）时，才允许手动输出 `<video>`，并在代码旁用中文注释说明原因。

### 背景色（每个板块都应包含）

使用 `color_background` 类型，兼容纯色和渐变，不使用 `color` 类型：

```json
{ "type": "header", "content": "板块背景" },
{
  "type": "color_background",
  "id": "bg_color",
  "label": "背景色",
  "default": "#FFFFFF"
}
```

> 每个板块的 `default` 值各自设定，无统一默认值。

Liquid 输出：

```liquid
<style>
  .section-{{ section.id }} {
    --prefix-bg: {{ section.settings.bg_color }};
  }
</style>
```

```css
.section-{{ section.id }}.q-{name} {
  background: var(--prefix-bg, #ffffff);
}
```

### 上下边距（每个板块都应包含）

```json
{ "type": "header", "content": "上下边距" },
{ "type": "range", "id": "padding_top_mobile", "label": "上边距（移动端）", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 40 },
{ "type": "range", "id": "padding_bottom_mobile", "label": "下边距（移动端）", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 40 },
{ "type": "range", "id": "padding_top_desktop", "label": "上边距（桌面端）", "min": 0, "max": 200, "step": 4, "unit": "px", "default": 60 },
{ "type": "range", "id": "padding_bottom_desktop", "label": "下边距（桌面端）", "min": 0, "max": 200, "step": 4, "unit": "px", "default": 60 }
```

Liquid 输出：

```liquid
<style>
  .section-{{ section.id }} {
    --padding-top-mobile: {{ section.settings.padding_top_mobile }}px;
    --padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;
    --padding-top-desktop: {{ section.settings.padding_top_desktop }}px;
    --padding-bottom-desktop: {{ section.settings.padding_bottom_desktop }}px;
  }
</style>
```

```css
.section-{{ section.id }}.q-{name} {
  padding-top: var(--padding-top-mobile, 40px);
  padding-bottom: var(--padding-bottom-mobile, 40px);
}
@media screen and (min-width: 750px) {
  .section-{{ section.id }}.q-{name} {
    padding-top: var(--padding-top-desktop, 60px);
    padding-bottom: var(--padding-bottom-desktop, 60px);
  }
}
```

### 字段类型注意事项

| 类型 | 默认值要求 | 备注 |
|------|-----------|------|
| `color` | 必须 hex 值 | **不推荐**，改用 `color_background` |
| `color_background` | hex 或 gradient 字符串 | **推荐**，兼容渐变，适用背景色、强调色 |
| `range` | 必须在 min/max 内 | 必须提供 `unit` |
| `url` | 必须 `"/"` | 不能为空 |
| `image_picker` | 不需要 | Liquid 中用 `!= blank` 判断 |

### 响应式断点

项目只有一个断点：**750px**（移动端 / 桌面端）

```css
/* 移动端样式（默认） */
.section-{{ section.id }}.q-{name} { ... }

/* 桌面端样式 */
@media screen and (min-width: 750px) {
  .section-{{ section.id }}.q-{name} { ... }
}
```

---

## 六、Presets 预设数据规范

运营拖拽板块时需要立即看到展示效果，因此 **presets 必须包含完整的默认 blocks 数据**：

```json
"presets": [
  {
    "name": "Q-{Name}",
    "blocks": [
      {
        "type": "card",
        "settings": {
          "title": "默认标题",
          "description": "默认描述文字，运营可替换",
          "button_label": "Shop Now",
          "button_link": "/"
        }
      }
    ]
  }
]
```

**注意**：
- 图片类字段（`image_picker`）无法在 presets 中预设，留空即可
- 文字、链接、数字类字段必须填充有意义的占位内容

---

## 七、加入页面模板

编辑 `templates/*.json`，在 `sections` 对象中添加板块，并在 `order` 数组中指定位置：

**开发阶段**：新增板块放到 `order` 最前面，方便直接预览，`main`（产品主体）放最后：

```json
{
  "sections": { ... },
  "order": ["q-{name}", "...", "main"]
}
```

**上线前**：根据实际页面顺序调整 `order`，再将 `main` 移回合适位置。

---

## 八、验证规范

- **日常小修改**：运行 `pnpm dev` 在浏览器查看效果即可
- **较大改动（新增 section / 修改 Schema 结构）**：使用 Shopify Dev MCP 验证

```javascript
// 仅在较大改动时调用
CallMcpTool({ server: "user-shopify-dev-mcp", toolName: "learn_shopify_api", arguments: { api: "liquid" } })
CallMcpTool({
  server: "user-shopify-dev-mcp",
  toolName: "validate_theme",
  arguments: {
    conversationId: "...",
    absoluteThemePath: "/Users/zhushaoxian/Documents/项目/anran",
    filesCreatedOrUpdated: [{ path: "sections/q-{name}.liquid" }]
  }
})
```

---

## 九、常见报错与解决方案

### 1. `{%- liquid %}` 块内不支持布尔表达式赋值

**报错信息**：
```
Liquid syntax error (line XX): Expected end_of_string but found comparison in "{{tab_blocks.size > 0}}"
```

**原因**：`{%- liquid %}` 标签内的 `assign` 不支持直接赋值布尔比较表达式。

```liquid
❌ 错误写法
{%- liquid
  assign has_tabs = tab_blocks.size > 0
-%}

✅ 正确写法
{%- liquid
  assign has_tabs = false
  if tab_blocks.size > 0
    assign has_tabs = true
  endif
-%}
```

---

### 2. 模板 JSON 报「分区类型不能引用现有分区文件」

**报错信息**：
```
分区类型"q-xxx"不能引用现有分区文件
```

**原因**：`templates/*.json` 引用了该 section，但 section 文件本身存在语法错误，导致 Shopify 无法识别。

**解决步骤**：先修复 section 文件的语法错误，section 上传成功后模板报错会自动消失。

---

### 3. Schema JSON 无效

**报错信息**：
```
"schema"标记中的 JSON 无效
```

**原因**：`{% schema %}` 块内的 JSON 格式有误（多余逗号、引号错误、字段名拼写错等）。

**排查方式**：将 schema 内容复制到 [jsonlint.com](https://jsonlint.com) 验证，定位具体行号。

---

### 4. `inline_richtext` / `richtext` 字段不允许空字符串

**报错信息**：
```
设置的"header_title"无效。所有顶级节点必须是 '<p>'、'<ul>'、'<ol>' 或 '<h1>'-'<h6>' 个标签
```

**原因**：`templates/*.json` 中为 `inline_richtext` 或 `richtext` 类型字段赋值了空字符串 `""`。Shopify 会对这两种类型做 HTML 格式验证，空字符串不是合法的富文本内容，上传时报错。

**解决方案**：在模板 JSON 中**不传该字段**（Shopify 会自动回退到 schema default 值），而不是传空字符串。

```json
❌ 错误 - 空字符串导致报错
"settings": {
  "header_title": ""
}

✅ 正确 - 不想显示标题时，直接省略该 key
"settings": {
  "header_center": true
}
```

**注意**：
- `text`、`textarea` 类型可以安全地传空字符串，限制仅针对 `inline_richtext` 和 `richtext` 类型。
- `inline_richtext` 与 `richtext` 在 **schema default** 和 **模板 JSON 实际值**中的规则**完全相反**，容易混淆：

| 位置 | `inline_richtext` | `richtext` |
|------|-------------------|------------|
| schema `default` | 纯文本（不能含块级标签） | 需用 `<p>` 等块级标签包裹 |
| 模板 JSON 实际值 | 纯文本或行内 HTML（`<strong>`、`<em>` 等），**禁止** `<p>` 等块级标签 | 需用 `<p>` 等块级标签包裹 |

> ⚠️ Shopify 对 `inline_richtext` 严格执行"不允许块级标签"，在 templates JSON 中写 `<p>` 会报 **"标记'<p>'是不允许的"** 上传错误。

```json
// schema default — inline_richtext 用纯文本（两者一致）
{ "type": "inline_richtext", "id": "header_title", "default": "Why Choose Anran?" }

// ❌ 错误 — inline_richtext 在模板 JSON 中写 <p> 会报错
"header_title": "<p>Why Choose Anran?</p>"

// ✅ 正确 — inline_richtext 在模板 JSON 中只写纯文本或行内标签
"header_title": "Why Choose Anran?"
"header_title": "Why Choose <strong>Anran</strong>?"
```

---

### 5. `range` 字段的 `default` 必须是 `step` 的整数倍

**报错信息**：
```
Invalid schema: setting with id="xxx" default 必须是范围内的一个步骤
```

**原因**：`range` 类型的 `default` 值必须能被 `step` 整除，且落在 `min`～`max` 范围内。若 `default` 不是 `step` 的整数倍，Shopify 上传时会直接报错拒绝。

**示例**：

```json
❌ 错误 - 739 不是 step:10 的整数倍（739 / 10 = 73.9）
{
  "type": "range",
  "id": "image_height_desktop",
  "min": 200,
  "max": 900,
  "step": 10,
  "unit": "px",
  "default": 739
}

✅ 正确 - 740 是 step:10 的整数倍
{
  "type": "range",
  "id": "image_height_desktop",
  "min": 200,
  "max": 900,
  "step": 10,
  "unit": "px",
  "default": 740
}
```

**规则**：`(default - min) % step === 0`

常见 step 对应可用 default 示例：

| step | 合法 default 示例 | 非法 default 示例 |
|------|-----------------|-----------------|
| 1 | 任意整数 | - |
| 2 | 20, 22, 24... | 21, 23... |
| 4 | 40, 44, 48... | 41, 42, 43... |
| 10 | 200, 210, 740... | 739, 741... |

**从 Figma 设计稿取值时尤其注意**：设计稿的精确像素值（如 739px）往往不是 step 的整数倍，需手动取近似值（739 → 740）。

---

### 6. `richtext` / `inline_richtext` 的 presets 预设值不允许内联 `style` 属性

**报错信息**：
```
Invalid preset "xxx": invalid block type "yyy":
设置的"description"无效。属性"style="color: #xxxxxx""包含标签"<span>"上不允许使用的样式。
```

**原因**：Shopify 对 `presets`（以及 `templates/*.json`）中 `richtext` / `inline_richtext` 字段的 HTML 值做安全校验，**不允许出现内联 `style` 属性**（如 `style="color: #1565d8"`）。上传时直接报错拒绝。

**解决方案**：presets 预设值中去掉所有内联 `style`，改用语义标签（`<strong>`、`<em>`）；颜色效果通过 CSS class 或 CSS 变量在 section 样式中实现。

```json
❌ 错误 - presets 中含内联 style，上传报错
{
  "type": "card",
  "settings": {
    "description": "<p><span style=\"color: #1565d8\">No door opening required.</span></p>"
  }
}

✅ 正确 - 去掉内联 style，改用语义标签
{
  "type": "card",
  "settings": {
    "description": "<p><strong>No door opening required.</strong></p>"
  }
}
```

**颜色效果的替代方案**：

```css
/* 在 section CSS 中针对特定语义标签定义颜色 */
.section-{{ section.id }} .q-xxx__card-desc strong {
  color: #1565d8;
}
```

**注意**：
- 此限制仅针对 `presets` 和 `templates/*.json` 中的**实际赋值**，schema `default` 值也同样受此限制
- 商户在 Shopify 后台主题编辑器中**手动输入**的富文本可以正常保存内联样式，限制仅在代码预设值中生效
- `text`、`textarea` 类型无此限制，只有 `richtext` 和 `inline_richtext` 受影响

---

### 7. `range` 字段的 `max` 必须严格小于 10000

**报错信息**：
```
Invalid schema: setting with id="xxx" max 必须小于 10000
```

**原因**：Shopify 对 `range` 类型的 `max` 值有上限约束，**max 必须严格小于 10000**（即 max ≤ 9999）。等于 10000 时上传直接报错。

**解决方案**：将 `max` 改为不超过 9999 的值，并确保 `default` 仍是 `step` 的整数倍。

```json
❌ 错误 - max 等于 10000，上传报错
{
  "type": "range",
  "id": "autoplay_delay",
  "min": 2000,
  "max": 10000,
  "step": 500,
  "unit": "ms",
  "default": 5000
}

✅ 正确 - max 改为 9500（< 10000，且 (9500-2000)/500 = 15 为整数）
{
  "type": "range",
  "id": "autoplay_delay",
  "min": 2000,
  "max": 9500,
  "step": 500,
  "unit": "ms",
  "default": 5000
}
```

**常见受影响字段**：任何以毫秒（ms）为单位、且想设到 10000ms 的 `range`（如自动播放间隔）。

---

### 8. `templates/*.json` 中 block 类型无效

**报错信息**：
```
块"xxx"的类型值无效。类型必须在架构中定义。
```

**原因一（级联错误）**：section schema 本身有错（如第 3、5、7 条）→ Shopify 无法解析 schema → 找不到 block type → templates 里所有引用该 section 的 block 均报此错。  
**原因二（时序问题）**：向 `templates/*.json` 手动内联写入 block 定义时，Shopify admin 做运行时校验，若 section 文件尚未上传或为新增文件，服务端 schema 里找不到该 block type，即使本地代码正确也会报错。

**根本原因**：Shopify admin 在验证 `templates/*.json` 时会对照**服务端已上传的** section schema 校验 block type。若 section 是全新文件，服务端还没有它的 schema，即使本地 schema 正确也会报错。

**正确推送顺序（新 section + 带 blocks 的 template）**：
- **方案 A（推荐）**：同一次 `shopify theme push` 同时推送 section 文件和 templates/*.json，Shopify 会整批处理，无时序问题。
- **方案 B**：先推送 section 文件（templates 里暂不加 blocks），确认 section 上传成功后，再推送带 block 定义的 templates/*.json。

```json
✅ 正确 - 同次推送，block ID 用语义字符串，inline_richtext 用 <p> 包裹
{
  "sections": {
    "q-hero-banner": {
      "type": "q-hero-banner",
      "blocks": {
        "hero-slide-1": {
          "type": "slide",
          "settings": {
            "title": "Pro Wireless Security Camera for Home",
            "show_rating": true,
            "button_label": "Shop with Free App",
            "button_link": "/"
          }
        }
      },
      "block_order": ["hero-slide-1"],
      "settings": {}
    }
  },
  "order": ["q-hero-banner"]
}
```

**若仍报错**，按以下顺序排查：
1. 先修复 section 文件的 schema 错误（JSON 语法、range 默认值/max 上限等）
2. 确认 `"type"` 字段与 schema `blocks[].type` **完全一致（区分大小写）**
3. 确认 `inline_richtext` / `richtext` 字段值用 `<p>` 包裹，不含内联 `style`

---

## 十、按钮交互状态规范

所有按钮必须添加 `hover` / `active` 交互颜色，提升操作反馈。统一规则如下：

### 渐变背景按钮（Tab 激活态）

渐变色无法直接过渡，用 `opacity` 模拟 hover 变暗效果：

```css
/* 激活态 hover：降低透明度 */
.section-{{ section.id }} .q-pr__tab-btn--active:hover {
  opacity: 0.85;
}
```

### 纯色边框按钮（Tab 未激活态）

```css
.section-{{ section.id }} .q-pr__tab-btn {
  transition: background 0.2s, color 0.2s, border-color 0.2s, opacity 0.2s;
}

/* hover：浅色填充 + 边框加深 */
.section-{{ section.id }} .q-pr__tab-btn:hover:not(.q-pr__tab-btn--active) {
  background: rgba(108, 188, 231, 0.12);
  border-color: #367cf6;
  color: #367cf6;
}

/* active：加深填充 */
.section-{{ section.id }} .q-pr__tab-btn:active:not(.q-pr__tab-btn--active) {
  background: rgba(108, 188, 231, 0.25);
}
```

### 纯色填充按钮（Shop Now / CTA）

```css
.section-{{ section.id }} .q-pr__shop-btn {
  transition: background 0.2s, opacity 0.2s;
}

/* hover：颜色加深约 10% */
.section-{{ section.id }} .q-pr__shop-btn:hover {
  background: #e8940f; /* #faa61a 加深 */
}

/* active：整体降低透明度 */
.section-{{ section.id }} .q-pr__shop-btn:active {
  opacity: 0.8;
}
```

### 颜色加深参考值

| 原色 | hover 加深色 | 说明 |
|------|-------------|------|
| `#faa61a`（橙色） | `#e8940f` | CTA 按钮 |
| `#2d4e76`（深蓝） | `#1e3550` | 深色按钮 |
| 渐变色 | `opacity: 0.85` | 无法直接过渡，用透明度替代 |

---

## 十一、Judge.me 评分接入规范

项目已全局引入 Judge.me，产品卡片中的评分区统一使用以下方式：

### 推荐写法（有关联产品时）

```liquid
<div class="jdgm-widget jdgm-preview-badge" data-id="{{ product.id }}">
  {{- product.metafields.judgeme.badge -}}
</div>
```

- `jdgm-preview-badge`：Judge.me 的星级 + 评论数徽章组件，自动渲染
- `data-id`：必须传入 `product.id`，Judge.me 用此匹配评论数据

### 降级写法（无关联产品 / 纯手动配置时）

当卡片没有关联 Shopify 产品时，用 `block.settings.review_count` 手写文字降级：

```liquid
{%- if product != blank -%}
  <div class="jdgm-widget jdgm-preview-badge" data-id="{{ product.id }}">
    {{- product.metafields.judgeme.badge -}}
  </div>
{%- elsif block.settings.review_count != blank -%}
  {{- block.settings.review_count -}}
{%- endif -%}
```

### CSS 适配

Judge.me badge 内部结构需适配卡片行高：

```css
.section-{{ section.id }} .jdgm-prev-badge {
  display: flex;
  align-items: center;
  gap: 4px;
}

.section-{{ section.id }} .jdgm-prev-badge__stars {
  font-size: 12px; /* 移动端 */
}

.section-{{ section.id }} .jdgm-prev-badge__text {
  font-size: 10px;
  color: #1e1e1e;
}
```

### 注意事项

- **不要**在产品卡片中手写 SVG 星级，关联产品时一律用 Judge.me badge
- `product.metafields.judgeme.badge` 输出的是完整 HTML，直接 `{{ }}` 输出即可，无需过滤器
- 完整评论列表组件用 `product.metafields.judgeme.widget`（用于评论专区板块，非产品卡片）

---

## 十二、卡片列表布局选择规范

在开发卡片列表类布局时，**必须先判断使用场景**，再决定用 Swiper 还是普通 Grid/Flex。

### 选择决策树

```
卡片数量是否固定（由设计稿决定，不依赖运营配置块数）？
├── 是 → 用 Grid
└── 否 → 移动端是否需要横向滑动？
          ├── 是 → 用 Swiper
          └── 否 → 用 Grid
```

### 使用 Swiper 的条件（必须同时满足）

1. **移动端需要横向滑动**：卡片超出屏幕宽度，需要左右滑动浏览
2. **卡片数量不固定**：blocks 数量由运营在后台任意增减，PC 端也无法保证整行排满

典型场景：
- 产品横向滚动展示（移动端一屏显示 1.x 张，提示可滑动）
- 评论/评测卡片数量不定，移动端逐张滑动

### 使用 Grid 的条件（满足任一即可）

1. **移动端垂直堆叠**：卡片在移动端上下排列（每行 1 列），不需要滑动
2. **PC 端固定列数**：设计稿明确规定 PC 端几列（如 3 列、4 列），移动端也是对应的整数列
3. **全部卡片同时可见**：无论移动端还是 PC 端，所有卡片都展示在视口内，不存在隐藏内容

### 禁止行为

- **禁止**在移动端垂直堆叠场景使用 Swiper（用 Grid 代替）
- **禁止**在卡片数量固定且设计稿明确列数时使用 Swiper
- **禁止**用 Swiper 仅仅是因为"可能有多张卡片"——先看移动端是否真的需要滑动

---

## 十三、swiper-container 使用规范

所有轮播功能**必须**使用 `swiper-container` Web Component，禁止手写原生 JS 初始化 Swiper。仅在满足上方「十二、卡片列表布局选择规范」中 Swiper 使用条件时采用。

### 基础结构

```liquid
<swiper-container
  slides-per-view="1"
  space-between="16"
  grab-cursor="true"
  loop="false"
  navigation="true"
  navigation-prev-el="[data-carousel-prev='{{ section.id }}']"
  navigation-next-el="[data-carousel-next='{{ section.id }}']"
  pagination="true"
  pagination-el="[data-carousel-pagination='{{ section.id }}']"
  pagination-clickable="true"
  breakpoints='{"750":{"slidesPerView":3,"spaceBetween":24}}'
>
  {%- for block in section.blocks -%}
    <swiper-slide {{ block.shopify_attributes }}>
      <!-- 卡片内容 -->
    </swiper-slide>
  {%- endfor -%}
</swiper-container>

{%- if section.blocks.size > 1 -%}
  <div class="q-xxx__nav">
    <button
      class="q-xxx__nav-btn q-xxx__nav-btn--prev"
      data-carousel-prev="{{ section.id }}"
      aria-label="Previous"
      tabindex="0"
    ><!-- 左箭头 SVG --></button>
    <button
      class="q-xxx__nav-btn q-xxx__nav-btn--next"
      data-carousel-next="{{ section.id }}"
      aria-label="Next"
      tabindex="0"
    ><!-- 右箭头 SVG --></button>
  </div>
  <div data-carousel-pagination="{{ section.id }}"></div>
{%- endif -%}
```

### 必须配套的 CSS

`swiper-container` 设置 `display: flex` + `gap`，`swiper-slide` 设置 `flex-shrink: 0`，Swiper 才能正确计算布局：

```css
.section-{{ section.id }} swiper-container {
  display: flex;
  gap: 14px; /* 移动端间距，与 space-between 保持一致 */
}

@media screen and (min-width: 750px) {
  .section-{{ section.id }} swiper-container {
    gap: 24px; /* 桌面端间距，与 breakpoints spaceBetween 保持一致 */
  }
}

.section-{{ section.id }} swiper-slide {
  flex-shrink: 0;
}
```

> `gap` 值必须与 `swiper-container` 属性中的 `space-between` / `breakpoints spaceBetween` 保持一致。

### 关键规则

| 规则 | 说明 |
|------|------|
| **swiper-container 设置 flex + gap** | 必须添加 `display: flex` 和 `gap`，值与 `space-between` 保持一致 |
| **swiper-slide 设置 flex-shrink: 0** | 防止 slide 被压缩，Swiper 才能正确计算布局 |
| **不设置 slide 宽度** | `slides-per-view` 为数字时 Swiper 自动计算，不要给 `swiper-slide` 设置 `width` |
| **导航按钮选择器带 section.id** | `[data-carousel-prev='{{ section.id }}']`，防止多个同类板块互相干扰 |
| **分页器同理** | `[data-carousel-pagination='{{ section.id }}']` |
| **导航/分页按需使用** | 只有一张 slide 时不渲染导航和分页（用 `blocks.size > 1` 判断） |
| **breakpoints 用 JSON 字符串** | `breakpoints='{"750":{"slidesPerView":3,"spaceBetween":24}}'` |
| **禁止手动调用 swiper.slidePrev/Next** | 导航由 Swiper 原生接管，Islands JS 无需处理导航点击 |

### 常用属性速查

| 属性 | 说明 | 示例值 |
|------|------|--------|
| `slides-per-view` | 每屏显示张数 | `"1"` / `"auto"` |
| `space-between` | slide 间距（px） | `"16"` |
| `loop` | 循环播放 | `"true"` / `"false"` |
| `grab-cursor` | 鼠标抓手样式 | `"true"` |
| `autoplay-delay` | 自动播放间隔（ms） | `"3000"` |
| `navigation` | 开启导航 | `"true"` |
| `navigation-prev-el` | 上一张按钮选择器 | `"[data-carousel-prev='{{ section.id }}']"` |
| `navigation-next-el` | 下一张按钮选择器 | `"[data-carousel-next='{{ section.id }}']"` |
| `pagination` | 开启分页器 | `"true"` |
| `pagination-el` | 分页器容器选择器 | `"[data-carousel-pagination='{{ section.id }}']"` |
| `pagination-clickable` | 分页点可点击 | `"true"` |
| `pagination-type` | 分页器类型 | `"bullets"` / `"fraction"` |
| `breakpoints` | 响应式断点配置 | `'{"750":{"slidesPerView":3}}'` |

---

## 十四、Figma 设计稿读取规范

当任务同时提供了电脑端和移动端设计稿时，**必须分别读取两份设计稿**，提取各自的尺寸、间距、字号、布局数据，**不得只参考其中一份**。

每次开发新板块，用户会同时提供**电脑端**和**移动端**两份设计稿链接。**必须分别读取两份设计稿**，不能只读其中一份。

### 读取顺序

```
1. get_design_context(电脑端节点)   → 提取桌面端所有数值
2. get_design_context(移动端节点)   → 提取移动端所有数值
3. 对照两份稿子，分别写入对应断点的 CSS
```

### 需要从两份设计稿分别提取的数值

| 数值类型 | 桌面端（≥750px） | 移动端（默认） | CSS 写法 |
|----------|----------------|--------------|---------|
| 板块上下内边距 | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 内容区左右内边距 | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 标题字号 | Figma 标注值 | Figma 标注值 | `header_title_size_desktop/mobile` |
| 描述字号 | Figma 标注值 | Figma 标注值 | `header_desc_size_desktop/mobile` |
| 标题行高 | Figma 标注值 | Figma 标注值 | `line-height` |
| 标题下边距（到内容区） | Figma 标注值 | Figma 标注值 | `header_margin_bottom_desktop/mobile` |
| 卡片间距（gap） | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 卡片圆角 | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 卡片内边距 | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 每行列数 | Figma 标注值 | Figma 标注值 | `grid-template-columns` + `@media` |
| 图片宽高比 | Figma 标注值 | Figma 标注值 | `aspect-ratio` |
| 按钮圆角 / 内边距 | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 按钮字号 | Figma 标注值 | Figma 标注值 | CSS 变量 + `@media` |
| 各文字颜色 | Figma 标注值 | Figma 标注值 | 统一提取，两端通常相同 |

### CSS 双端写法模板

所有从设计稿提取的数值，一律通过 CSS 变量注入，并在 `@media` 中覆盖桌面端值：

```css
/* CSS 变量注入（Liquid style 块中） */
.section-{{ section.id }} {
  /* 移动端默认值 */
  --gap: 12px;
  --card-padding: 12px 16px;
  --card-radius: 8px;
  --title-size: 16px;
  --title-line-height: 1.4;
  --desc-size: 13px;
  --btn-padding: 10px 20px;
  --btn-radius: 6px;
  --btn-font-size: 14px;
  --cols: 1;
}

@media screen and (min-width: 750px) {
  .section-{{ section.id }} {
    /* 桌面端覆盖 */
    --gap: 24px;
    --card-padding: 20px 24px;
    --card-radius: 12px;
    --title-size: 22px;
    --title-line-height: 1.3;
    --desc-size: 15px;
    --btn-padding: 14px 32px;
    --btn-radius: 8px;
    --btn-font-size: 16px;
    --cols: 3;
  }
}

/* 使用变量 */
.section-{{ section.id }} .q-xxx__card {
  padding: var(--card-padding);
  border-radius: var(--card-radius);
}

.section-{{ section.id }} .q-xxx__grid {
  display: grid;
  grid-template-columns: repeat(var(--cols), 1fr);
  gap: var(--gap);
}
```

### 注意事项

- **不能只看一端**：桌面端和移动端字号、间距、列数往往相差很大，必须两份都读
- **设计稿像素值取近似值**：如稿子标 739px，schema `range` 的 `default` 需取 `step` 整数倍（→ 740）
- **颜色两端通常一致**：只需读一遍，但仍需确认
- **布局结构差异**：移动端可能是单列 + 轮播，桌面端是多列网格，结构不同时需要单独的 HTML 或通过 CSS 切换显示
- **图片宽高比**：从设计稿中提取实际宽高比（如 `4/3`、`1/1`），两端可能不同
- **轮播仅移动端**：若移动端用轮播、桌面端用网格，在 Liquid 中条件渲染或用 CSS `display:none` 切换

---

## 十五、在现有 Section 中新增 Block 块

适用于**已有 `blocks` 的 Section**（如 `main-product.liquid`、`main-product-v2.liquid`），在不动整体结构的前提下新增一种可拖拽的 block 类型。新建独立板块（q-* section）不适用本节，按「一、Section 文件结构」从零定义 blocks 即可。

> ⚠️ **main-product-v2 强制规范**：在 `main-product-v2.liquid`（及同类型产品主 section）中**新增**的 block，**必须**先在 `snippets` 中新增 snippet 文件，再在 section 中通过 `{% render 'snippet名', block: block %}` 引用；**禁止**在 section 内直接书写该 block 的 HTML。既便于维护，也保持 section 主文件简洁。

**main-product-v2 新增 block 涉及文件：**

| 文件 | 说明 |
|------|------|
| `snippets/product-block-{block_type}.liquid` | 新建：该 block 的 HTML 与逻辑，根元素带 `{{ block.shopify_attributes }}` |
| `sections/main-product-v2.liquid` | 修改：schema 的 `blocks` 中新增一条定义；`case block.type` 中新增 `when` + `render` 引用上述 snippet |

### 操作步骤概览（main-product-v2 新增 block）

1. 在 `{% schema %}` 的 `blocks` 数组中新增一条 block 定义。
2. **在 `snippets` 中新增 snippet 文件**（如 `snippets/product-block-{block_type}.liquid`），在该文件中书写该 block 的完整 HTML 与逻辑，根元素上输出 `{{ block.shopify_attributes }}`。
3. 在 Section 的 `{% for block in section.blocks %}` / `{% case block.type %}` 中**仅**新增一行 `{% when '块类型' %}` + `{% render 'product-block-xxx', block: block %}`，不写块内容 HTML。
4. 若该 block 需在页面两处渲染（如主栏 + 弹窗），弹窗部分可在 section 中单独循环渲染，或由同一 snippet 通过参数控制输出不同片段。

### 1. 在 Schema 中定义 Block

在 `{% schema %}` 的 `"blocks": [ ... ]` 数组中追加一个对象，**保持与现有 block 相同的缩进与逗号**：

```json
{
  "type": "my_block",
  "name": "我的块（或 t: 翻译 key）",
  "limit": 1,
  "settings": [
    { "type": "text", "id": "heading", "label": "标题", "default": "标题" },
    { "type": "richtext", "id": "content", "label": "内容" }
  ]
}
```

| 字段 | 说明 |
|------|------|
| `type` | 块类型唯一标识，**必须**与 Liquid 中 `when 'my_block'` 的字符串一致，建议小写+下划线 |
| `name` | 主题编辑器中显示的名称，可用 `t:sections.xxx.blocks.my_block.name` |
| `limit` | 可选；该类型最多可添加个数，如 `1` 表示仅允许添加一个 |
| `settings` | 该 block 的配置项数组，与 section `settings` 写法一致 |

**注意**：`settings` 内字段仍须遵守本规范中的「五、Schema 标准配置项」「九、常见报错」等（如 `range` 的 default 为 step 整数倍、`richtext`/`inline_richtext` 在 presets/模板 JSON 中不用空字符串、不含内联 `style`）。

### 2. main-product-v2：Section 内仅引用 Snippet

在 `main-product-v2.liquid` 中，**只**在 `{% case block.type %}` 里增加一个分支并调用 snippet，不写块内容：

```liquid
{%- for block in section.blocks -%}
  {%- case block.type -%}
    {%- when 'my_block' -%}
      {% render 'product-block-my-block', block: block %}
    {%- when 'text' -%}
      <!-- 其他已有 block ... -->
  {%- endcase -%}
{%- endfor -%}
```

Snippet 命名建议：`product-block-{block_type}.liquid`，与 schema 中的 `type` 对应（如 `type: "my_block"` → `product-block-my-block.liquid`）。若已有惯例（如 `icon-with-text`）可沿用。

### 3. Snippet 内书写 Block 结构与 shopify_attributes

在 `snippets/product-block-my-block.liquid`（或你命名的 snippet）中写该 block 的完整 HTML，**最外层元素必须带** `{{ block.shopify_attributes }}`：

```liquid
{%- comment -%} 供 main-product-v2 使用，传入 block -%}
<div class="product__my-block" {{ block.shopify_attributes }}>
  {%- if block.settings.heading != blank -%}
    <h3 class="product__my-block-heading">{{ block.settings.heading | escape }}</h3>
  {%- endif -%}
  {%- if block.settings.content != blank -%}
    <div class="product__my-block-content rte">{{ block.settings.content }}</div>
  {%- endif -%}
</div>
```

**必须遵守**：

- Snippet 的**根元素**上必须写 `{{ block.shopify_attributes }}`，否则主题编辑器中无法选中、拖拽该块。
- 使用 `block.settings.xxx` 读取配置；snippet 通过 `render 'name', block: block` 接收，变量名为 `block`。
- 若该 section 使用 `.section-{{ section.id }}` 做样式隔离，block 的样式选择器需加 `.section-{{ section.id }}` 前缀（样式可写在 section 的 `<style>` 或共用 CSS 文件中）。

### Snippet 内使用轮播、视频、图片时与 Section 同规

在 product block 的 snippet 中若使用 **swiper-container**、**视频**、**图片**，须与 Section 开发遵守相同规则，不单独放宽或手写实现。

- **图片**：一律使用 `image_url` + `image_tag`，设置正确的 `loading`、`widths`/`sizes`、`alt`。禁止手写 `<img>`。详见「五、Schema 标准配置项 → 图片与视频输出规范」。
- **视频**：一律使用 `video_tag`，通过 `image_size` 控制尺寸。禁止手写 `<video>`；仅当 `video_tag` 无法满足需求时方可手写，并加中文注释说明原因。同上规范。
- **轮播**：一律使用 `swiper-container` Web Component，禁止手写原生 Swiper 初始化。导航按钮用 `data-carousel-prev` / `data-carousel-next`，分页用 `data-carousel-pagination`；选择器须带 section 维度（如 `[data-carousel-prev='{{ section_id }}']`）以免同页多块冲突。配套 CSS：`swiper-container` 设 `display: flex` + `gap`，`swiper-slide` 设 `flex-shrink: 0`。详见「十三、swiper-container 使用规范」。  
  Snippet 内无 `section` 对象时，由 section 在 `render` 时传入 `section_id`，例如：`{% render 'product-block-xxx', block: block, section_id: section.id %}`，snippet 内用传入的 `section_id` 写导航/分页选择器及 CSS 中的 `.section-{{ section_id }}` 前缀。

### 4. Block 需在页面两处渲染时（如弹窗）

若 block 既在信息栏显示触发器，又在页面别处渲染内容（如 `popup` 的 modal），做法：

1. **主循环**：在 `case block.type` 中只渲染触发器（如链接/按钮），并带上 `{{ block.shopify_attributes }}`。
2. **二次渲染**：用 `assign` 按类型过滤后单独循环，在合适 DOM 位置输出内容（如 modal 容器内）：

```liquid
{% assign popups = section.blocks | where: 'type', 'popup' %}
{%- for block in popups -%}
  <modal-dialog id="PopupModal-{{ block.id }}" ... {{ block.shopify_attributes }}>
    <!-- 弹窗内容，使用 block.settings -->
  </modal-dialog>
{%- endfor -%}
```

同一 block 在两次循环中都可使用 `{{ block.shopify_attributes }}`，主题编辑器会正确关联。

其他 Section（非 main-product-v2）若某 block 的 HTML 或逻辑较多，也可抽成 snippet，主 section 只保留 `when` + `render 'snippet名', block: block`；snippet 根元素上须输出 `{{ block.shopify_attributes }}`。

### 5. 与 App block（@app）的区别

- `type: "@app"` 的 block 由 App 提供，用 `{% render block %}` 即可，无需在 schema 里写 `settings`。
- 自定义 block 的 `type` 不要以 `@` 开头，在 schema 中显式定义 `type`、`name`、`settings`（及可选 `limit`），并在 case 中写 `when 'your_type'` 对应渲染。

### 检查清单（新增 Block 时）

- [ ] 在 `{% schema %}` 的 `blocks` 数组中新增完整 block 对象（`type`、`name`、可选 `limit`、`settings`）。
- [ ] `type` 与 Liquid 中 `when '...'` 字符串完全一致。
- [ ] **main-product-v2**：在 `snippets` 中新增 snippet 文件（如 `q-block-{type}.liquid`），section 内**仅**写 `{% when 'xxx' %}` + `{% render 'q-block-xxx', block: block %}`，不在 section 内写该 block 的 HTML。 样式写在新增 snippet 文件中。 block name前面要加q-。 不需要图片替代文字配置
- [ ] 该 block 在页面上输出的**最外层包裹元素**带有 `{{ block.shopify_attributes }}`（在 snippet 根元素上）。
- [ ] 若需两处渲染（如主栏 + 弹窗），两处都按需输出且不遗漏 `block.shopify_attributes`。
- [ ] block 的 `settings` 遵守 range default、richtext/inline_richtext、无内联 style 等规范。
- [ ] 样式选择器加 `.section-{{ block.id }}` 前缀，不污染其他 section。
- [ ] Snippet 内若使用图片、视频、轮播：图片用 `image_url` + `image_tag`，视频用 `video_tag`，轮播用 `swiper-container`，且轮播导航/分页选择器带 block 维度（必要时通过 `block_id` 传入）；与 Section 规范一致。 image_tag: 配置时不需要配置widths、sizes、alt

---

## 检查清单

创建新 Section 时确保：
- [ ] **头部使用 `{%- render 'q-section-header', section: section -%}`，禁止自行编写标题/描述 HTML**
- [ ] **Schema settings 包含完整的通用头部字段（header_title、header_description 等 11 个字段）**
- [ ] **同时读取电脑端和移动端两份 Figma 设计稿**
- [ ] 从两份设计稿分别提取：字号、行高、间距、圆角、列数、图片比例
- [ ] 移动端数值作为默认值，桌面端数值写在 `@media (min-width: 750px)` 中
- [ ] Schema 包含 `bg_color`（color_background 类型）
- [ ] Schema 包含上下边距四个 range 字段
- [ ] Presets 包含完整的默认 blocks 数据（拖入即可展示）
- [ ] **Presets / templates JSON 中 richtext/inline_richtext 字段值不含内联 `style` 属性**
- [ ] 所有 CSS 选择器加 `.section-{{ section.id }}` 前缀
- [ ] 背景色/强调色用 `color_background` 类型，不用 `color`
- [ ] `url` 类型字段必须提供 `"default": "/"`
- [ ] Islands JS 文件放 `frontend/islands/`，自定义元素用 `client:visible`
- [ ] 响应式只用 750px 一个断点
- [ ] 轮播功能使用 `swiper-container` Web Component，导航用 `data-carousel-prev/next='{{ section.id }}'`，分页用 `data-carousel-pagination='{{ section.id }}'`
- [ ] 所有按钮添加 hover/active 交互颜色变化（纯色加深、渐变用 opacity）
- [ ] 产品卡片评分使用 Judge.me badge（关联产品时），无产品时用手写文字降级
- [ ] 图片设置正确的 `loading` 和 `sizes` 属性

### 性能强制项

- [ ] 排在 `templates/index.json` 第 1 位的 section 内首屏图片：`loading: 'eager'` + `fetchpriority: 'high'`；其余图片：`loading: 'lazy'`
- [ ] 轮播中第 1 张 slide 背景图：`loading: 'eager'`；其余 slides：`loading: 'lazy'`
- [ ] 所有交互 Islands 使用了 `client:visible`（首屏 Hero 也适用，observer 立即触发）
- [ ] CSS 动画使用 `transform` / `opacity`，不对 `width` / `height` / `top` / `left` 做动画
- [ ] `swiper-container` 设 `width: 100%; display: block`，`swiper-slide` 设 `width: 100%`（防 CLS）
- [ ] 图片容器设 `aspect-ratio` 或 `padding-top` 百分比，防止图片加载时撑开页面
- [ ] `will-change` 仅在动画元素上使用，禁止全局或非必要场景使用

### 自动测试强制项

- [ ] **开发完成后**，使用 `user-chrome-devtools` MCP 对 `http://127.0.0.1:9292/` 执行 navigate → screenshot → list_console_messages(error/warn) 完整流程
- [ ] 确认无 Shopify Upload Error（红色横幅）
- [ ] 确认自己代码无 JS 错误（排除已知 Shopify/第三方可忽略错误，见项目通用规范）
- [ ] 截图确认页面视觉渲染正确（布局无错位、无白屏、无 CLS 闪烁）
